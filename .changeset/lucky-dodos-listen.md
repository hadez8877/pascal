---
'pascal-vscode': minor
---

Adds a new `Pascal Solar` theme light variant!

Select it from the Command Palette via **Color Theme → Pascal Solar**, or set it directly:

```json
{
  "workbench.colorTheme": "Pascal Solar"
}
```
